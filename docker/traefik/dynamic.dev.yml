http:
  routers:
    traefik_router:
      rule: "Host(`traefik.mangkoran.xyz`)"
      service: "api@internal"
      tls:
        domains:
          - main: "mangkoran.xyz"
            sans:
              - "*.mangkoran.xyz"
    webserver_router:
      rule: "Host(`ppiedufest.mangkoran.xyz`)"
      service: webserver_service
      tls:
        domains:
          - main: "mangkoran.xyz"
            sans:
              - "*.mangkoran.xyz"
    phpmyadmin_router:
      rule: "Host(`phpmyadmin.mangkoran.xyz`)"
      service: phpmyadmin_service
      tls:
        domains:
          - main: "mangkoran.xyz"
            sans:
              - "*.mangkoran.xyz"
  services:
    webserver_service:
      loadBalancer:
        servers:
          - url: "http://webserver"
    phpmyadmin_service:
      loadBalancer:
        servers:
          - url: "http://phpmyadmin"

tls:
  certificates:
    - certFile: "/etc/traefik/certs/local-cert.pem"
      keyFile: "/etc/traefik/certs/local-key.pem"
